apiVersion: v1
kind: Service
metadata:
  name: pyroscope-distributor
  namespace: pyroscope
  labels:
    app.kubernetes.io/name: pyroscope
    app.kubernetes.io/component: distributor
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 4040
      targetPort: http
  selector:
    app.kubernetes.io/name: pyroscope
    app.kubernetes.io/component: distributor
